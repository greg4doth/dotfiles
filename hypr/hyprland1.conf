
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor= eDP-1,2560x1600@60,1920x0, 1.66666
monitor = HDMI-A-1,1920x1080@165,0x0, 1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fm = yazi
$browser = google-chrome-stable
$secbrowser = firefox
$menu = fuzzel
#$menu = wofi --show run
$tg = materialgram
#$settings = vim /home/nekochem/.config/hypr/hyprland.conf
$obsidian = obsidian
$vpn = kitty --class v2raya -e sudo v2raya 
#$logout = wlogout
$logout = wlogout
#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar
exec-once = hyprpaper 
exec-once = fnott
exec-once = clipse -listen # its clipboard
exec-once = wl-clipboard 
#exec-once = hyprpanel
#exec-once = v2raya
exec-once = systemctl --user start hyprpolkitagent




#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,18
env = HYPRCURSOR_SIZE,18

#env = ELECTRON_OZONE_PLATFORM_HINT,x11 #for obsidian
env = ELECTRON_OZONE_PLATFORM_HINT,auto #?? (its working)

#env = GTK_THEME=Materia-dark


env = QT_QPA_PLATFORM,wayland

env = QT_QPA_PLATFORMTHEME,qt5ct

env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1

env = QT_AUTO_SCREEN_SCALE_FACTOR,1

env = QT_STYLE_OVERRIDE,kvantum

env = GDK_BACKEND,wayland,x11,*

env = CLUTTER_BACKEND,wayland 

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2 
    gaps_out = 0

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(c0caf5ee) rgba(b4f9f8ee) 45deg
    col.inactive_border = rgba(1a1b26ee)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false
#
    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
snap {

}

}

#plugins#

plugin:dynamic-cursors {

    enabled = true

    # sets the cursor behaviour, supports these values:
    # tilt    - tilt the cursor based on x-velocity
    # rotate  - rotate the cursor based on movement direction
    # stretch - stretch the cursor shape based on direction and velocity
    # none    - do not change the cursors behaviour
    mode = stretch

    # minimum angle difference in degrees after which the shape is changed
    # smaller values are smoother, but more expensive for hw cursors
    threshold = 2

    # override the mode behaviour per shape
    # this is a keyword and can be repeated many times
    # by default, there are no rules added
    # see the dedicated `shape rules` section below!
    shaperule = <shape-name>, <mode> (optional), <property>: <value>, ...
    shaperule = <shape-name>, <mode> (optional), <property>: <value>, ...

    # for mode = rotate
    rotate {

        # length in px of the simulated stick used to rotate the cursor
        # most realistic if this is your actual cursor size
        length = 20

        # clockwise offset applied to the angle in degrees
        # this will apply to ALL shapes
        offset = 0.0
    }

    # for mode = tilt
    tilt {

        # controls how powerful the tilt is, the lower, the more power
        # this value controls at which speed (px/s) the full tilt is reached
        limit = 5000

        # relationship between speed and tilt, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used (most realistic to actual air drag)
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        function = negative_quadratic
    }

    # for mode = stretch
    stretch {

        # controls how much the cursor is stretched
        # this value controls at which speed (px/s) the full stretch is reached
        limit = 3000

        # relationship between speed and stretch amount, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        function = quadratic
    }

    # configure shake to find
    # magnifies the cursor if its is being shaken
    shake {

        # enables shake to find
        enabled = true

        # use nearest-neighbour (pixelated) scaling when shaking
        # may look weird when effects are enabled
        nearest = true

        # controls how soon a shake is detected
        # lower values mean sooner
        threshold = 6.0

        # magnification level immediately after shake start
        base = 4.0
        # magnification increase per second when continuing to shake
        speed = 4.0
        # how much the speed is influenced by the current shake intensitiy
        influence = 0.0

        # maximal magnification the cursor can reach
        # values below 1 disable the limit (e.g. 0)
        limit = 0.0

        # time in millseconds the cursor will stay magnified after a shake has ended
        timeout = 2000

        # show cursor behaviour `tilt`, `rotate`, etc. while shaking
        effects = false

        # enable ipc events for shake
        # see the `ipc` section below
        ipc = false
    }

    # use hyprcursor to get a higher resolution texture when the cursor is magnified
    # see the `hyprcursor` section below
    hyprcursor {

        # use nearest-neighbour (pixelated) scaling when magnifing beyond texture size
        # this will also have effect without hyprcursor support being enabled
        # 0 / false - never use pixelated scaling
        # 1 / true  - use pixelated when no highres image
        # 2         - always use pixleated scaling
        nearest = true

        # enable dedicated hyprcursor support
        enabled = true

        # resolution in pixels to load the magnified shapes at
        # be warned that loading a very high-resolution image will take a long time and might impact memory consumption
        # -1 means we use [normal cursor size] * [shake:base option]
        resolution = -1

        # shape to use when clientside cursors are being magnified
        # see the shape-name property of shape rules for possible names
        # specifying clientside will use the actual shape, but will be pixelated
        fallback = clientside
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 3
    #rounding_power = 3.0 #?deleteed?

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.9

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 8
        passes = 1

        vibrancy = 0.1696
    }
}

 



# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us, ru 
    kb_variant = 
    kb_model =
    kb_options = grp:caps_toggle
    kb_rules =

    follow_mouse = 1
    repeat_rate	= 27
    repeat_delay = 250
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    scroll_method = on_button_down 
    natural_scroll = 0

    scroll_button = 1
    scroll_button_lock = 0 #its ohuenno	
    #disable_while_typing = true
   # touchpad {
    #    natural_scroll = true
#	disable_while_typing = true
			
	
#    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
:gestures {
    workspace_swipe = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
devdice {
    name = epic-mouse-v1
    sensitivity = -0.5
}
device {
    name = gxt7863:00-27c6:01e0-touchpad
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    scroll_method = on_button_down 
    natural_scroll = 1
    scroll_button = 1
    scroll_button_lock = 0 #its ohuenno	
    }
device {
     name = gxt7863:00-27c6:01e0-mouse

    }

#device {
 #   name = GXT7863:00 27C6:01E0 
 #   natural_scroll = true
#	disable_while_typing = true
#	natural_scroll = true 
 #  clickfinger_behavior = true
  #  }

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$altMod = ALT 
$shiftMod = SHIFT 

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, G, exec, $browser
#bind = $mainMod, I, exec, $settings
bind = $mainMod, M, exit,
#bind = $mainMod, E, exec, $fileManager
#bind = $mainMod, V, togglefloating,
bind = $mainMod, D, exec, $menu
#bind = $mainMod , $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, Y, togglesplit, # dwindle
#bind = $mainMod, L, exec, hyprlock

bind = $mainMod, O, exec, $logout
bind = $mainMod, F, togglefloating
bind = $mainMod, W, centerwindow




#window managment

bind = Super, H, movefocus, l # [hidden]
bind = Super, L, movefocus, r # [hidden]
bind = Super, K, movefocus, u # [hidden]
bind = Super, J, movefocus, d # [hidden]


#my apps
#bind = $mainMod, V, exec, cliphist list | wofi --dmenu | cliphist decode |

bind = $mainMod, V, exec, kitty --class clipse -e clipse 
#bind =  $mainMod SHIFT, O, exec, $terminal --class hyprpicker
bind = $mainMod, E, exec, kitty --class yazi -e yazi
#
#bind = $altMod, V, exec, kitty --class sudo v2raya -e sudo  v2raya

#i need alt
bind = $altMod, F, exec, $secbrowser
bind = $mainMod, A, fullscreen, 1
bind = $mainMod  $shiftMod, A, fullscreen, 0
bind = $altMod, P, exec, kitty --class timr -e timr

bind = $altMod, C, exec,  hyprpicker -a 
#bind = $mainMod, X, resizewindowpixel, 700 700, class:(sioyek)#
#bind = $mainMod, X, movewindowpixel, 3 35, class:(sioyek)	
#bind = $ma

bind = $altMod, T, exec,  $tg
bind = $altMod, O, exec, $obsidian
#bind = $altMod, V, exec, $vpn
bind = $altMod, V, exec, $vpn 

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10


#### Screenshot a window
##bind = $mainMod, PRINT, exec, hyprshot -m window
### Screenshot a monitor
##bind = , PRINT, exec, hyprshot -m output
### Screenshot a region
##bind = $shiftMod, PRINT, exec, hyprshot -m region
#(its working like a hueta)


#trying from yt
bind =  $mainMod $shiftMod, S, exec, grim -g "$(slurp -d)" -|wl-copy


# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

windowrulev2 = float, class:(clipse)
windowrulev2 = size 762 457, class:(clipse)
#windowrulev2 = dimaround, on, class:(clipse)
windowrulev2 = bordercolor  rgba(c0caf5ee)  class:(clipse)
#windowrulev2 = col.inactive_border rgba(81ec86ee) class:(clipse) 
windowrulev2 = float, class:(yazi)
windowrulev2 = size 1200 750,  class:(yazi)

#windowrulev2 = float, class:(hyprpicker)
#windowrulev2 = size 350 350, class:(hyprpicker)

#windowrulev2 = move: 4 38 title:(Meet)(.*

#windowrule = float, title:(Meet ) (.*)

windowrulev2 = float, class:(obsidian)
windowrulev2 = suppressevent maximize, class:(obsidian) 
#windowrulev2 = size  , class:(obsidian)

#windowrulev2 = float, class:(org.telegram.desktop) 
#windowrulev2 = size 1143 798, class:(org.telegram.desktop)
#windowrulev2 = move: 196 42, class:(org.telegram.desktop)
windowrulev2 = suppressevent maximize, class:((org.telegram.desktop))
windowrulev2 = float, class:(startwine)
windowrulev2 = suppressevent maximize, class:(startwine)
#windowrulev2 = float, class: ()
windowrulev2 = float, class:(sioyek)
windowrulev2 = size 1200 750, class: (sioyek)
windowrulev2 = float, class:(waydroid)
windowrulev2 = float, class:(ncpamixer)
#windowrulev2 = size 682 324, class:(ncpamixer)
#windowrulev2 = move: 771 35, class:(ncpamixer)
#windowrulev2 = float, title:(Media viewer)
windowrulev2 = float, class:(vesktop)
#windowrulev2 = , class:(vesktop)

#windowrulev2 = suppressevent maximize, class:(sioyek)
windowrulev2 = float, class:(timr)
windowrulev2 = size 760 300, class:(timr)
windowrulev2 = float, class:(impala)
windowrulev2 = size 1528 455, class:(impala)
windowrulev2 = move: 4 38, class:(impala)
windowrulev2 = float, class:(ncpamixer)
windowrulev2 = size 760 455, class:(ncpamixer) 
windowrulev2 = move: 388 38, class:(ncpamixer) 
windowrulev2 = float, class:(tenki)
windowrulev2 = move: 772 38, class:(tenki)
windowrulev2 = size 760 224, class:(tenki)
# Fix some dragging issues with XWayland
windowrulev2 = move: 4 38, class:(calcurse)
windowrulev2 = float, class:(calcurse)
windowrulev2 = size 760 455 class:(calcurse)
windowrulev2 = size 1144 455 class:(htop)
windowrulev2 = float, class:(htop)
windowrulev2 = move: 204 38 class:(htop)

windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
windowrulev2 = float, class:(org.qbittorrent.qBittorrent)
windowrulev2 = suppressevent maximize, class:(org.qbittorrent.qBittorrent)

windowrulev2 = float, class:(dev.benz.walker)
#windowrulev2 = float, class:(v2raya)
windowrulev2 = suppressevent maximize, class:(v2raya)
windowrulev2 = float, title:(Открытие файла)
#windowrulev2 = suppressevent maximize, title:(Открытие файла)
windowrulev2 = float, class:(Yad)
windowrule = float, title:(.*)(Enhancer for YouTube™ — Mozilla Firefox)
#windowrulev2 = fullscreen, 1,  class:(io.github.kukuruzka165.materialgram)
#windowrulev2 = float, class:(io.github.kukuruzka165.materialgram)
windowrulev2 = fullscreen,[1] title:(Media viewer)
windowrulev2 = float, class:(v2raya)
windowrulev2 = size 500 200, class:(v2raya)
windowrulev2 = float, class:(xdg-desktop-portal-gtk)
#windowrulev2 = float, class:(io.github.kukuruzka165.materialgram)
#windowrulev2 = fullscreenstate 1,  class:(io.github.kukuruzka165.materialgram)
#layerrule = rule, aMedia viewerddress
windowrulev2 = float, title:^(Библиотека)$
windowrulev2 = fullscreenstate 1, title:^(Библиотека)$
windowrulev2 = fullscreenstate 1, class:(xdg-desktop-portal-gtk)
windowrulev2 = float, class:(Thunar)
